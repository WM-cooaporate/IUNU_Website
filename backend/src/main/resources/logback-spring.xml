<?xml version="1.0" encoding="UTF-8"?>
<!--
  Production writes one JSON object per line to stdout, which is what Render
  and every log platform ingest without a parser. Each line carries requestId
  (from RequestIdFilter) and logger_name, so "logger_name:SECURITY" and
  "requestId:<uuid>" are field searches rather than regular expressions.

  Every other profile keeps Spring Boot's usual plain-text console output.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- Only the MDC key this app sets on purpose; nothing a
                     library might put there ends up in the log by accident. -->
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <version>[ignore]</version>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
                <customFields>{"service":"iunu-backend"}</customFields>
                <!-- A stack trace is one field, bounded, root cause first. -->
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerThrowable>40</maxDepthPerThrowable>
                    <maxLength>8192</maxLength>
                    <rootCauseFirst>true</rootCauseFirst>
                </throwableConverter>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
